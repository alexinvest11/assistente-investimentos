name: Dica Diária Europa (desativado)

# Removido a pedido do usuário (tributação sobre rendimentos no exterior)
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow desativado."
