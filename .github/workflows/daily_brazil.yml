name: Dica Diária Brasil (desativado)

# Desativado — substituído pelo Monitor Mercado (a cada 2h)
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow foi desativado. Use Monitor Mercado."
